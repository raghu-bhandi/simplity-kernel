<?xml version="1.0" encoding="UTF-8"?>
<service xmlns="http://www.simplity.org/schema" name="rowExists"
	moduleName="tutorial" dbAccessType="readOnly" description="Test actions row exists" >
	<!-- <inputData>
		<inputRecords>
			specify the record name with purpose as read. A field with name of 
				primary key is expected in the input

			<inputRecord recordName="rec.Employees" purpose="read"></inputRecord>

		</inputRecords>
	</inputData> -->
	<actions>
		<setValue fieldName="employeeNumber" fieldValue="1056"></setValue>
		<rowExists recordName="birt.Employees" actionName="rowExistsAction"
			description="Check if row exists"  fieldName="employeeNumber" />
		<setValue fieldName="testValue" fieldValue="1234" executeOnCondition="rowExistsActionResult &gt; 0"></setValue>
	</actions>
	<outputData fieldNames="testValue"/>
	<!-- <outputData fieldNames="txt">
	<outputRecords>
	 all fields form the record are sent back
		<outputRecord recordName="birt.Employees" />
	comment the above an uncomment the below in case you want an array as response
	
		<outputRecord sheetName="customers"/>
		
	</outputRecords>
</outputData> -->
</service>
