/**
 * We have to deal with files in application. Programmers are used to having files on a disk. And
 * typically they assume, without thinking about it, that the same file system is available across
 * layers. We intend to provide set of utility lasses that help programmers to go ahead with such
 * assumptions, without worrying about deployment scenarios
 *
 * @author simplity.org
 */
package org.simplity.kernel.file;
